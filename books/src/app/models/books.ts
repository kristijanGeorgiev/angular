export interface Books {
    id: number;
    title: string;
    author: string;
    isbn: string;
    publishedYear: number;
    publisher: string;
    available: boolean;
    condition: string;
    language: string;
}